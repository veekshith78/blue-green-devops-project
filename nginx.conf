events {}

http {
    upstream backend {
        server blue:5000;
        # server green:5000;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://backend;
        }
    }
}